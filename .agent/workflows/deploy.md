---
description: Build and Deploy to Firebase Hosting
---

1. Run the deploy script
   // turbo
2. npm run deploy

Note: This runs `next build` followed by `firebase deploy`. Ensure you have authenticated with firebase CLI if needed (though the agent usually handles local variations).
